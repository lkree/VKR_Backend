export const CONFIG_PATH = '/assets/config/config.json';
export const SRC_CONFIG_PATH = '/assets/config/config.json';

export const NUMBER_SEPARATOR = ',';

export enum Routes {
  Login = '/login',
}

export enum AccessLevel {
  User = 'user',
  Admin = 'admin',
}
